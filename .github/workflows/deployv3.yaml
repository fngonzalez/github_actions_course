name: deployv3

on: push

jobs:
  first_job_v3:
    runs-on: ubuntu-latest

    steps:
      - name: Print variable
        run: echo "${{ secrets.NAME }}"

      - name: setup
        uses: actions/setup@v4
        with:
          node-version: '18'
